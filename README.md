# 🎒 SchoolHub — приложение для класса

Android · Kotlin · Jetpack Compose · Material 3 · MVVM · Room · Coroutines/Flow

| Модуль | Статус |
|---|---|
| 📚 Шпаргалки (текст + фото, предметы, поиск, избранное) | ✅ готово |
| 🔄 Обмен: интернет (REST) + без интернета (Bluetooth / Wi-Fi Direct) | ✅ готово |
| 🗓 Расписание + уведомления | 🔜 |
| ✅ Домашка | 🔜 |
| 🤖 Офлайн-LLM (llama.cpp) | 🔜 (проверка RAM уже работает) |
| 🌐 Офлайн-переводчик (ML Kit) | 🔜 |

## 📦 Как получить APK (GitHub Actions)
1. Создай на GitHub **пустой** репозиторий, например `SchoolHub`.
2. Загрузи туда **содержимое** этой папки (Add file → Upload files, перетащи все файлы и папки).
   ⚠️ Проверь, что загрузилась скрытая папка `.github/workflows/`. Если нет — создай
   файл `.github/workflows/build-apk.yml` через «Add file → Create new file» и вставь содержимое.
3. Открой вкладку **Actions** — сборка стартует сама (≈5–8 минут).
4. Готовый APK появится в **Releases**. Постоянная ссылка на свежую сборку:
   `https://github.com/<логин>/<репо>/releases/latest/download/SchoolHub.apk`

## 🔄 Как работает обмен шпаргалками
* **Offline-first**: всё хранится в Room на телефоне; сеть только догоняет.
* **Есть интернет и сервер** → автосинхронизация при старте, при появлении сети и после каждой правки.
* **Нет интернета** → «Обмен → Начать обмен с классом». Google Nearby Connections сам выбирает
  Bluetooth, BLE, Wi-Fi Direct или локальную сеть. Телефоны одного класса (одинаковый **код класса**)
  находят друг друга, обмениваются снимками базы и **пересылают новое дальше** — шпаргалка
  доходит до всех по цепочке.
* Конфликты: побеждает последняя правка (Last-Writer-Wins по `updatedAt`); удаление тоже распространяется.
* Полученное по Bluetooth потом само уходит на сервер, когда появится интернет.

### Сервер (необязательно)
`server/index.js` — Node 18+, без зависимостей:
```bash
cd server && node index.js     # слушает :8080 (или $PORT)
```
Можно бесплатно развернуть на Render/Railway/Fly.io или на ПК в школьной сети.
Адрес указывается в приложении: «Обмен → Адрес сервера класса».

## 🏗 Сборка локально
Android Studio Koala+ → Open → папка проекта (JDK 17). Или: `gradle assembleDebug` (Gradle 8.9).

Подробности архитектуры — в [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md).

## Что нового в 1.1.0
- Все разделы готовы: расписание со звонками и напоминаниями, домашка, оценки, фокус-таймер, настройки, ИИ, переводчик с камерой.
- Синхронизация шпор, расписания, звонков и ДЗ (интернет + Bluetooth/Wi-Fi Direct).
- 14 офлайн-моделей ИИ, включая новые Qwen 3, Gemma 3, Gemma 3n, Phi-4 mini, LFM2.
- У каждой модели: 🧠 «ум» 0–100, ⚠️ примерный % ошибок, 👁 «видит картинку целиком» или 📝 «читает только текст с фото».
- Фото в чат ИИ: текст распознаётся на телефоне (ML Kit) и вставляется в вопрос.
- Движок llama.cpp обновлён до b6550 (нужен для Qwen 3 / Gemma 3 / Gemma 3n / LFM2).

## v2.1 — аккаунты и админка (Firebase)
- Обязательная регистрация: почта + пароль (подтверждение письмом), Google, Apple. Гостевого входа нет.
- После Google/Apple — обязательный ввод настоящих имени и фамилии (серое предупреждение о блокировке).
- Вкладка «🛡 Админ» (только админам): статистика устройств, все аккаунты, блокировка по почте, назначение админов.
- Статистика доходит до админа через интернет/Wi-Fi (очередь Firestore) и по Bluetooth через телефоны класса.
- Настройки → «💧 Жидкое стекло» и выбор шрифта.
- Настройка Firebase: см. **FIREBASE-SETUP.md**, правила базы — **firestore.rules**.
